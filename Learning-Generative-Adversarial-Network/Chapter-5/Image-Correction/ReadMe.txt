The checkpoints directory has trained model of 20k iteration. If training the model from scratch is not feasible for you, you can directly execute the image  correction script pointing to this checkpoint with checkpoint number as 20000.


